
name: vxkit
description: "VxKit Pentest Tool by Hx"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Modules indispensables pour tes nouvelles fonctions :
  webview_flutter: ^4.7.0
  flutter/services:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
